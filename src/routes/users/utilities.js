"use strict";

import UserModel from "../../models/user";

const fetchUsers = () => {};

const createUser = ({}) => {};

const editUser = ({ userId, fieldToUpdate }) => {};

const suspendUser = ({ userId }) => {};

export { fetchUsers, createUser, editUser, suspendUser };
